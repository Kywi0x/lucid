// Config du viewer public — même source de vérité que l'app (src/lib/supabase-config).
export {
  DEFAULT_SUPABASE_URL as SUPABASE_URL,
  DEFAULT_SUPABASE_ANON_KEY as SUPABASE_ANON_KEY,
} from "@/lib/supabase-config";
